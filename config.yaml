region: us-east-1

function_name: lc_media_encode_input_to_ets
handler: service.handler
# role: lambda_basic_execution
description: Automatically trigger encode job when files are uploaded to bucket

# if access key and secret are left blank, boto will use the credentials
# defined in the [default] section of ~/.aws/credentials.
aws_access_key_id:
aws_secret_access_key:

# dist_directory: dist
# timeout: 15
# memory_size: 512
